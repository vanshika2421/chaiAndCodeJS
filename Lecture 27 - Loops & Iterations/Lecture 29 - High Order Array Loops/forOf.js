// const arr = [1,2,3,4,5,6];

// for (const num of arr) {
//     console.log(num);
// }

// names = "vanshikaKamra";
// for (const naming of names) //     console.log(naming);    
// }

// // WHAT IS MAP ??
// // map is an object which hold key value pairs.

// const map = new map();
// map.set('IN', "Indea");
// map.set('USA', "united states of america");
// map.set('Fr', "France");
// map.set('IN', "India");

// console.log("map");

// for (const [key,value] of map) {
//     console.log(key , ':-', value);   
// }

// const myObject = {
//     game1 : 'NFS',
//     game2 : 'spiderman', 

// }

const myObject = {
    js : 'javaScript',
    cpp : 'c++',
    rb : 'ruby',
    swift : "swift"
}

for (const key in object) {
    if (Object.hasOwnProperty.call(object, key)) {
        const element = object[key];  
    }
}

